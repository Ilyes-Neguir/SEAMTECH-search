# Alias kept so existing commands and documentation stay valid:
#   pip install -r requirements.txt -r requirements-local.txt
# (CI has used exactly that line since before this file was reconciled.)
#
# It used to re-declare every runtime package a second time with its own pins,
# including pytest==9.1.1 against requirements-dev.txt's pytest==8.4.1 — the
# conflicting-pins finding in the audit. Everything now resolves through one
# file, so there is exactly one version per package repo-wide.
-r requirements-dev.txt
