# Runtime dependencies — this is the only list the Docker image installs
# (Dockerfile: `pip install --no-cache-dir -r requirements.txt`).
#
# Every package here is declared EXACTLY once across the repo. Test and lint
# tooling lives in requirements-dev.txt; requirements-local.txt is an alias for
# it. That is deliberate: these three files used to re-declare the same
# packages at different versions (pytest 8.4.1 vs 9.1.1, a second copy of every
# runtime pin), so which version you got depended on which file you installed.
fastapi==0.141.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
pypdf==6.19.0
pdfplumber==0.11.10
python-docx==1.2.0
python-multipart==0.0.32
reportlab==4.4.3
openpyxl==3.1.5
boto3==1.35.90
redis==5.2.1
psycopg2-binary==2.9.10
# Lot F — IA locale (plan v3.0 §10.4/§17.2) : embeddings ONNX e5-small 384 et
# classifieur maison. SANS PyTorch (postes 8 Go, décision commanditaire 21/09) :
# onnxruntime pour l'inférence, tokenizers pour la tokenisation, numpy pour le
# classifieur et la normalisation. Les poids du modèle NE SONT PAS dans le
# dépôt (465 Mo mesurés : modèle non quantifié 448 Mo + tokenizer 17 Mo) :
# `python -m seamtech_search.ml.telecharger` les récupère explicitement
# (action opérateur, jamais au runtime).
numpy==2.4.6
onnxruntime==1.30.0
tokenizers==0.23.2
